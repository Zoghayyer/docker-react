language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t azoghayyer/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true azoghayyer/docker-react npm run test
